# Original idea by @ISnackable. Thanks to him for handling the hardest parts!
# https://github.com/ISnackable/YTCubePlus/blob/main/.github/workflows/Build.yml

name: Build and Release uYouEnhanced

on:
  workflow_dispatch:
    inputs:
      sdk_version:
        description: "iOS SDK Version"
        default: "17.5"
        required: true
        type: string
      uyou_version:
        description: "uYou Version"
        default: "3.0.4"
        required: true
        type: string
      decrypted_youtube_url:
        description: "Direct URL of the decrypted YouTube IPA"
        default: ""
        required: true
        type: string
      bundle_id:
        description: "Modify the bundle ID"
        default: "com.google.ios.youtube"
        required: true
        type: string
      app_name:
        description: "Modify the app name"
        default: "YouTube"
        required: true
        type: string
      commit_id:
        description: "(Optional) Commit ID to build at"
        default: ""
        required: false
        type: string
      upload_artifact:
        description: "Upload IPA as artifact"
        default: false
        required: false
        type: boolean
      create_release:
        description: "Create a draft release"
        default: true
        required: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build uYouEnhanced
    runs-on: macos-14
    permissions:
      contents: write

    steps:
      - name: Checkout Main
        uses: actions/checkout@v4
        with:
          path: main
          ref: ${{ github.event.inputs.commit_id || github.ref }}

      - name: Install Dependencies
        run: brew install ldid dpkg make

      - name: Set PATH environment variable
        run: echo "$(brew --prefix make)/libexec/gnubin" >> $GITHUB_PATH

      - name: Setup Theos
        uses: actions/checkout@v4
        with:
          repository: theos/theos
          ref: master
          path: theos
          submodules: recursive

      - name: SDK Caching
        id: SDK
        uses: actions/cache@v4
        env:
          cache-name: iOS-${{ inputs.sdk_version }}-SDK
        with:
          path: theos/sdks/
          key: ${{ env.cache-name }}
          restore-keys: ${{ env.cache-name }}

      - name: Download iOS SDK
        if: steps.SDK.outputs.cache-hit != 'true'
        run: |
          git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/aricloverALT/sdks/
          cd sdks
          git sparse-checkout set --no-cone iPhoneOS${{ inputs.sdk_version }}.sdk
          git checkout
          mv *.sdk $THEOS/sdks
        env:
          THEOS: ${{ github.workspace }}/theos

      - name: Setup Theos Jailed
        uses: actions/checkout@v4
        with:
          repository: qnblackcat/theos-jailed
          ref: master
          path: theos-jailed
          submodules: recursive

      - name: Install Theos Jailed
        run: |
          ./theos-jailed/install
        env:
          THEOS: ${{ github.workspace }}/theos

      - name: Vendor YouTubeHeader
        run: |
          TARGET="main/Tweaks/YouTubeHeader"
          rm -rf "$TARGET"
          mkdir -p "$TARGET"
          git clone --quiet --depth=1 https://github.com/aricloverEXTRA/YouTubeHeader.git "$TARGET"
          rm -rf "$TARGET/.git"
          ln -sf ${{ github.workspace }}/$TARGET $THEOS/include/YouTubeHeader
        env:
          THEOS: ${{ github.workspace }}/theos

      - name: Clone Submodules directly
        run: |
          python3 - << 'PYEOF'
          import subprocess, os, re, sys

          SKIP_PATHS = {"Tweaks/YouTubeHeader"}

          def try_clone(url, path, branch=None):
              cmd = ["git", "clone", "--quiet", "--depth=1", "--recurse-submodules"]
              if branch:
                  cmd += ["--branch", branch]
              cmd += [url, path]
              r = subprocess.run(cmd, capture_output=True, text=True)
              if r.returncode == 0:
                  print(f"  OK: {url}")
                  return True
              print(f"  FAIL ({url}): {r.stderr.strip()[:120]}")
              return False

          gitmodules = open("main/.gitmodules").read()
          blocks = re.split(r'\[submodule\s+"[^"]+"\]', gitmodules)[1:]

          for block in blocks:
              pm = re.search(r'path\s*=\s*(.+)', block)
              um = re.search(r'url\s*=\s*(.+)', block)
              bm = re.search(r'branch\s*=\s*(.+)', block)
              if not pm or not um:
                  continue
              path   = pm.group(1).strip()
              url    = um.group(1).strip()
              branch = bm.group(1).strip() if bm else None
              if path in SKIP_PATHS:
                  print(f"Skipping (vendored): {path}")
                  continue
              target = os.path.join("main", path)
              if os.path.isdir(target) and os.listdir(target):
                  print(f"Already exists: {path}")
                  continue
              os.makedirs(target, exist_ok=True)
              print(f"Cloning {path} ...")
              if not try_clone(url, target, branch):
                  print(f"  ERROR: Failed to clone {path} from {url}")
                  sys.exit(1)
          PYEOF

      - name: Patch missing YouTubeHeader stubs
        run: |
          python3 - << 'PYEOF'
          import os, re

          HEADER_DIR = "main/Tweaks/YouTubeHeader"
          os.makedirs(HEADER_DIR, exist_ok=True)

          SPECIAL_STUBS = {
              "YTICompactListItemThumbnailSupportedRenderers.h": (
                  "#pragma once\n"
                  "#import <Foundation/Foundation.h>\n"
                  "#import <YouTubeHeader/YTIIconThumbnailRenderer.h>\n"
                  "@interface YTICompactListItemThumbnailSupportedRenderers : NSObject\n"
                  "@property (nonatomic, readonly) BOOL hasIconThumbnailRenderer;\n"
                  "@property (nonatomic, strong) YTIIconThumbnailRenderer *iconThumbnailRenderer;\n"
                  "@end\n"
              ),
              "YTICompactListItemRenderer.h": (
                  "#pragma once\n"
                  "#import <Foundation/Foundation.h>\n"
                  "#import <YouTubeHeader/YTICompactListItemThumbnailSupportedRenderers.h>\n"
                  "#import <YouTubeHeader/YTIFormattedString.h>\n"
                  "@interface YTICompactListItemRenderer : NSObject\n"
                  "@property (nonatomic, readonly) BOOL hasThumbnail;\n"
                  "@property (nonatomic, strong) YTICompactListItemThumbnailSupportedRenderers *thumbnail;\n"
                  "@property (nonatomic, strong) YTIFormattedString *title;\n"
                  "@end\n"
              ),
              "YTHeaderLogoControllerImpl.h": (
                  "#pragma once\n"
                  "#import <UIKit/UIKit.h>\n"
                  "@class YTITopbarLogoRenderer;\n"
                  "@interface YTHeaderLogoControllerImpl : UIViewController\n"
                  "- (void)setTopbarLogoRenderer:(YTITopbarLogoRenderer *)renderer;\n"
                  "@end\n"
              ),
          }

          for filename, content in SPECIAL_STUBS.items():
              target = os.path.join(HEADER_DIR, filename)
              open(target, "w").write(content)
              print(f"  SPECIAL STUB: {filename}")

          needed = set()
          exts = {".h", ".m", ".x", ".xm", ".mm", ".c"}
          for root_dir in ["main/Sources", "main/Tweaks"]:
              if not os.path.exists(root_dir):
                  continue
              for dirpath, _, filenames in os.walk(root_dir):
                  for filename in filenames:
                      if os.path.splitext(filename)[1] not in exts:
                          continue
                      filepath = os.path.join(dirpath, filename)
                      try:
                          for line in open(filepath, errors="ignore"):
                              m = re.search(r'#import\s+<YouTubeHeader/([^>\n]+\.h)>', line)
                              if m:
                                  needed.add(m.group(1))
                      except Exception:
                          pass

          stub_count = 0
          for header in sorted(needed):
              target = os.path.join(HEADER_DIR, header)
              if os.path.exists(target):
                  continue
              class_name = os.path.splitext(header)[0]
              stub = "@class " + class_name + ";\n@interface " + class_name + " : NSObject\n@end\n"
              open(target, "w").write(stub)
              print(f"  STUB: {header}")
              stub_count += 1

          print(f"==> {stub_count} generic stub(s) generated")
          PYEOF

      - name: Prepare YouTube IPA
        id: prepare_youtube
        run: |
          wget "${{ inputs.decrypted_youtube_url }}" --quiet --no-verbose -O main/YouTube.ipa
          cd ${{ github.workspace }}/main
          mv YouTube.ipa YouTube.zip
          unzip -q YouTube.zip
          youtube_version=$(defaults read "$(pwd)/Payload/YouTube.app/Info" CFBundleVersion)
          echo "==> YouTube v${youtube_version} downloaded"
          sed -i '' "19s#.*#YOUTUBE_VERSION = ${youtube_version}#g" Makefile
          echo "youtube_version=${youtube_version}" >> $GITHUB_OUTPUT
        env:
          THEOS: ${{ github.workspace }}/theos

      - name: Fix compiling & Build Package
        id: build_package
        run: |
          cd ${{ github.workspace }}/main
          sed -i '' "1s#.*#export TARGET = iphone:clang:${{ inputs.sdk_version }}:14.0#g" Makefile
          sed -i '' "29s#.*#DISPLAY_NAME = ${{ inputs.app_name }}#g" Makefile
          sed -i '' "30s#.*#BUNDLE_ID = ${{ inputs.bundle_id }}#g" Makefile
          make package THEOS_PACKAGE_SCHEME=rootless IPA=Payload/YouTube.app FINALPACKAGE=1
          echo "package=$(ls -t packages | head -n1)" >> $GITHUB_OUTPUT
          echo -e "==> \033[1mSHASUM256: $(shasum -a 256 packages/*.ipa | cut -f1 -d' ')\033[0m"
          echo -e "==> \033[1mBundle ID: ${{ inputs.bundle_id }}\033[0m"
        env:
          THEOS: ${{ github.workspace }}/theos
          UYOU_VERSION: ${{ inputs.uyou_version }}

      - name: Upload Artifact
        if: ${{ inputs.upload_artifact }}
        uses: actions/upload-artifact@v4
        with:
          name: uYouEnhanced_${{ steps.prepare_youtube.outputs.youtube_version }}_${{ inputs.uyou_version }}
          path: ${{ github.workspace }}/main/packages/${{ steps.build_package.outputs.package }}
          if-no-files-found: error

      - name: Create Release
        if: ${{ inputs.create_release }}
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ steps.prepare_youtube.outputs.youtube_version }}-${{ inputs.uyou_version }}-(${{ github.run_number }})
          name: v${{ steps.prepare_youtube.outputs.youtube_version }}-${{ inputs.uyou_version }}-(${{ github.run_number }})
          files: main/packages/*.ipa
          draft: true
